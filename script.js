const Gameboard = (() => {
  const board = ["", "", "", "", "", "", "", "", ""];

  const getBoard = () => {
     
  };

  const makeMove = (index, playerSymbol) => {
    
  };

  const resetBoard = () => {
    
  };

  return { getBoard, makeMove, resetBoard };
})();

